1. What if a delisted agent tries to do something that they are not allowed to do? Maybe something catastrophic?
2. Can anyone deploy the assetManager contract?
3. If they does, what if they maliciously get the `initializeDiamondCut` function to do anything they want?
4. Can call any external contracts as AssetManager?
5. How malign it could be for the system by not having any access control on the constructor of `AssetManager` contract?

```js
// Attacker's malicious init contract
contract MaliciousInit {
    function initialize() external {
        // Running in AssetManager's context via delegatecall

        // Drain any ETH
        payable(attacker).transfer(address(this).balance);

        // Manipulate diamond storage to add backdoor facets
        // Grant themselves admin roles
        // Approve tokens to themselves
        // etc.
    }
}

// Attacker deploys:
new AssetManager{value: 1 ether}(
    [], // empty cuts
    address(maliciousInit),
    abi.encodeWithSelector(MaliciousInit.initialize.selector)
);
```

6. Can there be a way for a malicious agent to update `withdrawalWaitMinSeconds` to 0 to withdraw instantly?
