1. What if a delisted agent tries to do something that they are not allowed to do? Maybe something catastrophic?
2. Can anyone deploy the assetManager contract?
3. If they does, what if they maliciously get the `initializeDiamondCut` function to do anything they want?
4. Can call any external contracts as AssetManager?
5. How malign it could be for the system by not having any access control on the constructor of `AssetManager` contract?

```js
// Attacker's malicious init contract
contract MaliciousInit {
    function initialize() external {
        // Running in AssetManager's context via delegatecall

        // Drain any ETH
        payable(attacker).transfer(address(this).balance);

        // Manipulate diamond storage to add backdoor facets
        // Grant themselves admin roles
        // Approve tokens to themselves
        // etc.
    }
}

// Attacker deploys:
new AssetManager{value: 1 ether}(
    [], // empty cuts
    address(maliciousInit),
    abi.encodeWithSelector(MaliciousInit.initialize.selector)
);
```

6. Can there be a way for a malicious agent to update `withdrawalWaitMinSeconds` to 0 to withdraw instantly?
7. Can the liquidations calls and challenges calls be front run by MEV bots? Finding a way to exploit the liquidators and reward seekers?
8. Find a way to see if the fullLiquidation can somehow be stopped by the agent?
9. Can there be a way for a challenger to not get rewarded for a successful challenge?
10. Can there be a way for a challenger to submit the wrong proof and still get rewarded?
11. let's say an agent has maliciously withdrawn all the underlying assets and before someone could report that, the agent front runs the challenge and announced withdrawal of collateral. Is this possible?
12. There is no access control on `AssetManagerController::updateContracts` function. Need to see how catastrophic can this be?
13. For the paused state of asset manager, need to make sure that all the core opeartions like minting and redemptions are not possible.
14. `agentVaultCollateralData` function is reading the balance directly from the vault address. This can be manipulated by donation attacks. Is this a problem?
15. Is the caller of `updateBalance` responsible for converting the amount into `UBA` (Underlying Balance Amount) units before passing it to this function?
16. Check if setEscrowFinished is updating the state correctly and if there can be a way to exploit this.
17. How does the agent knows that for a specific redemption request, the underlying assets need to be sent to the vault?
18. Are there any checks to ensure that an agent can not set the `extendedTimeStamp` to a very large number to delay the redemption processing indefinitely?
